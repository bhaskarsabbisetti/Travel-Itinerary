pipeline {
	agent any
		
	stages {
		stage('checkout') {
			steps {
				checkout scm
		stage('Build') {
    			steps {
        			bat '''
        			cd travel\\itinerary\\backend
        			python -m pip install --upgrade pip
			        python -m pip install -r requirements.txt
        			'''
				 }
				}

			}
		}
	}
}